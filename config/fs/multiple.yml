var_d: VAR_d
var_f: VAR_f

cmd:
  - exec:
      - cp -a ../asserts/. ../out/
    dir: .
  - exec:
      - tree
    dir: .

fs:
  - test_dir
  - test_dir_2

cmd_finish:
  - exec:
      - tree
    dir: .

